name: RepeatMasker
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # repeatmasker + rmblast
  # repeatmasker 4.1.5 is broken right now... hard pin to 4.1.2
  - repeatmasker==4.1.2.p1
  - rmblast
prefix: /global/scratch/users/stacy-l/miniconda3/envs/repeatmasker
