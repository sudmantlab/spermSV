name: process_variants
channels:
  - bioconda
  - conda-forge
dependencies:
  - python>=3.9
  - pandas==2.1.4
  - pytables
  - samtools
  - bedtools==2.31.0
  - bcftools==1.19
  - pysam
  - sniffles==2.2
  - trgt==0.7.0
  - cyvcf2
  # required for trgt outside of conda mgmt
  # cvbio depends on openjdk version pin
  # - openjdk==11.0.15
  # - cvbio
prefix: /global/scratch/users/stacy-l/miniconda3/envs/process_variants