name: spermSV
channels:
  - bioconda
  - conda-forge
dependencies:
  # set up a base analysis environment with only core packages
  - samtools
  - bedtools==2.31.1
  - seqtk
  - pybind11
  # - vcflib==1.0.10 # not be available on bioconda yet... manual install from source
  - python>=3.9
  - numpy
  - pandas
  - snakemake-minimal
prefix: /global/scratch/users/stacy-l/miniconda3/envs/spermSV